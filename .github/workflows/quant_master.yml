name: Quant Intelligence Master

on:
  schedule:
    # 1. 台股盤後分析 (台北 15:30) -> UTC 07:30
    - cron: '30 07 * * 1-5'
    
    # 2. 美股盤後分析 (台北 06:00) -> UTC 22:00
    - cron: '00 22 * * 1-5'
    
  workflow_dispatch: 

jobs:
  quant_tasks:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      # 💡 注意：GitHub Actions 環境的系統時間是 UTC
      - name: Execute Task
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          NEWS_WEBHOOK_URL: ${{ secrets.NEWS_WEBHOOK_URL }}
        run: |
          HOUR=$(date +%H)
          
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            # 手動執行：全部跑一遍
            python scripts/news_radar.py
            python scripts/ai_tw_post.py
            python scripts/ai_us_post.py
          else
            # 依據 UTC 時間自動判斷執行對象
            if [ "$HOUR" == "07" ]; then
              # --- 台北 15:30：台股盤後分析 ---
              python scripts/news_radar.py
              python scripts/ai_tw_post.py
              
            elif [ "$HOUR" == "22" ]; then
              # --- 台北 06:00：美股盤後分析 ---
              python scripts/news_radar.py
              python scripts/ai_us_post.py
            fi
          fi

      - name: Commit and Push
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/*.csv
          # 加上 || true 確保即使沒有檔案更動也不會報錯導致流程失敗
          git commit -m "Update history data [skip ci]" || true
          git push
