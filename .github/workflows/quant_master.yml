name: Quant Intelligence Master

on:
  schedule:
    # 每小時：新聞 / 系統狀態
    - cron: '0 * * * *'

    # 台股（UTC 07:30 = 台北 15:30）
    - cron: '30 07 * * 1-5'

    # 美股（UTC 22:00 = 台北 06:00）
    - cron: '00 22 * * 1-5'

  workflow_dispatch:

jobs:
  quant_tasks:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install Dependencies
        run: pip install -r requirements.txt

      - name: Execute Quant System
        env:
          # 🧠 AI 頻道（分市場）
          DISCORD_WEBHOOK_TW: ${{ secrets.DISCORD_WEBHOOK_TW }}
          DISCORD_WEBHOOK_US: ${{ secrets.DISCORD_WEBHOOK_US }}

          # 🔔 系統 / 新聞 / Dashboard（共用）
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          NEWS_WEBHOOK_URL: ${{ secrets.NEWS_WEBHOOK_URL }}
          BLACK_SWAN_WEBHOOK_URL: ${{ secrets.BLACK_SWAN_WEBHOOK_URL }}

          L4_ACTIVE_FILE: data/l4_active.flag

        run: |
          HOUR=$(date +%H)
          MINUTE=$(date +%M)
          NOW_TS=$(date +%s)
          OBS_FILE="data/l4_last_end.flag"

          echo "🕒 UTC $HOUR:$MINUTE | Event: ${{ github.event_name }}"

          SYSTEM_MODE="🟢 系統狀態：正常運作"

          if [ -f "$L4_ACTIVE_FILE" ]; then
            SYSTEM_MODE="🔴 系統狀態：黑天鵝防禦模式"
          elif [ -f "$OBS_FILE" ]; then
            LAST_END=$(cat $OBS_FILE || echo 0)
            DIFF=$((NOW_TS - LAST_END))
            if [ "$DIFF" -lt 86400 ]; then
              SYSTEM_MODE="🟠 系統狀態：風險觀察期"
            fi
          fi

          echo "$SYSTEM_MODE"

          # =========================
          # 🔔 每日系統狀態回報（UTC 00:00）
          # =========================
          if [ "$HOUR" == "00" ] && [ "$MINUTE" == "00" ]; then
            if [ -n "$DISCORD_WEBHOOK_URL" ]; then
              curl -s -X POST "$DISCORD_WEBHOOK_URL" \
                -H "Content-Type: application/json" \
                -d "{
                  \"content\": \"🧠 **量化系統每日狀態回報**\\n$SYSTEM_MODE\\n\\n📌 僅為風險與市場監控，非投資建議\\n🕒 UTC $(date '+%Y-%m-%d %H:%M')\"
                }"
            fi
          fi

          # =========================
          # 1️⃣ 新聞雷達（每小時）
          # =========================
          if [ "$MINUTE" == "00" ] || [ \"${{ github.event_name }}\" == \"workflow_dispatch\" ]; then
            python scripts/news_radar.py
          fi

          # =========================
          # 2️⃣ AI 分析（依市場）
          # =========================
          if [ \"${{ github.event_name }}\" == \"workflow_dispatch\" ]; then
            python scripts/ai_tw_post.py
            python scripts/ai_us_post.py
          else
            if [ \"$HOUR\" == \"07\" ] && [ \"$MINUTE\" == \"30\" ]; then
              python scripts/ai_tw_post.py
            elif [ \"$HOUR\" == \"22\" ] && [ \"$MINUTE\" == \"00\" ]; then
              python scripts/ai_us_post.py
            fi
          fi

          # =========================
          # 3️⃣ 績效 Dashboard（每日一次）
          # UTC 23:00 執行
          # =========================
          if [ \"$HOUR\" == \"23\" ] && [ \"$MINUTE\" == \"00\" ]; then
            python scripts/performance_dashboard.py
          fi

      # =========================
      # Commit & Push（非 L4）
      # =========================
      - name: Commit and Push
        run: |
          if [ -f data/l4_active.flag ]; then
            echo "🚨 L4 active — skip commit & push"
            exit 0
          fi

          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data/

          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update system data & metrics [skip ci]"
            git push
          fi
